name: M3U Daily Checker

# 1. CUÁNDO se ejecuta la acción:
on:
  schedule:
    # Se ejecuta todos los días a las 03:00 AM (hora UTC). 
    - cron: '0 3 * * *' 
  workflow_dispatch:
    # Permite ejecutar manualmente la acción

# 2. QUÉ hace la acción:
jobs:
  check_and_update:
    runs-on: ubuntu-latest 
    
    # SOLUCIÓN AL ERROR 403: Otorga permiso de escritura al bot para hacer el push
    permissions:
      contents: write 
      
    steps:
      - name: ⬇️ Checkout del Código
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: ⚙️ Instalar Librería Requests
        run: pip install requests
        
      - name: ⚡ Ejecutar Script de Verificación M3U (Multi-Archivo)
        run: python check_m3u.py
        
      - name: 📤 Commit y Push de Cambios
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          # CORRECCIÓN: Ahora añade todos los archivos M3U que el script ha sobrescrito.
          git add *.m3u
          # El '|| true' evita que falle si no hubo cambios en los archivos.
          git commit -m "🤖 Actualización automática de listas M3U (canales muertos eliminados)" || true
          git push
